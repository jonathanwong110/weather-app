# Weather-App

A simple single page application using vanilla JavaScript and a OpenWeather API to get the current weather.

## Installation

To install Weather-App

1) Fork and clone the repository. 
2) Open it in your editor. 
3) Open index.html. (One option includes entering explorer.exe index.html in your terminal) This will then open the application into your browser.
